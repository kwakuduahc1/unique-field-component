import { UniqueMrnDirective } from './unique-mrn.directive';

describe('UniqueMrnDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueMrnDirective();
    expect(directive).toBeTruthy();
  });
});
